{
  "name": "trials-test-performance-comparison-dashboard",
  "version": "1.0.0",
  "description": "A test dashboard to allow users to compare performance of crop trials incorporating user input prices for crop outputs",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && npm run dev",
    "build": "cd frontend && npm run build",
    "test": "cd frontend && npm test && cd ../backend && npm test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "agriculture",
    "trials",
    "performance",
    "dashboard",
    "crop",
    "comparison",
    "react",
    "nodejs"
  ],
  "author": "",
  "license": "ISC"
}